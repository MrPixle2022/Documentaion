<!-- @format -->

# Access modifiers:

access modifiers change the visibility of what it marks, we have:

- public: accessible anywhere in the project
- private: only in the class itself
- protected: class itself + it's sub classes

and by default -if you don't specify- the it's visible to the class, package, sub-classes of same package only
